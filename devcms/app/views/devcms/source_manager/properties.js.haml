$('.panel[data-level=child]').hide();
$('.panel_properties').slideRightShow();
$('#properties_form').find('input[name="id"]')[0].value = '#{@seo_id}';
-#$('.panel_structure .content').append("#{ escape_javascript(render(:partial => 'layout', :object => @source)) }");

